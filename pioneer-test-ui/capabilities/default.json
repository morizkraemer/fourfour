{
  "identifier": "default",
  "description": "Default capabilities for the test UI",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "core:event:default",
    "core:event:allow-listen",
    "core:event:allow-emit"
  ]
}
